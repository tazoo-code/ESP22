pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        flatDir {
            dirs 'lib'
        }
        google()
        mavenCentral()
    }
}


rootProject.name = "com.unipd.dei.esp22.appName"
include ':app'
