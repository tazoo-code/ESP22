<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unipd.dei.esp22.appName">
    <!-- premessi della fotocamera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--


 Limits app visibility in the Google Play Store to ARCore supported devices
         (https://developers.google.com/ar/devices).
    -->

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <uses-feature android:name="android.hardware.camera.ar" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Esp22" >

        <activity
            android:name="com.unipd.dei.esp22.appName.SettingsActivity"
            android:exported="false"
            android:configChanges="uiMode|locale|layoutDirection" />

        <activity
            android:name="com.unipd.dei.esp22.appName.MainActivity"
            android:exported="true"
            android:configChanges="uiMode|locale|layoutDirection" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.unipd.dei.esp22.appName.PlaneDetectionActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="com.unipd.dei.esp22.appName.AugmentedImagesActivity"
            android:exported="true" >
        </activity>

        <activity
            android:name="com.unipd.dei.esp22.appName.ImagesGalleryActivity"
            android:exported="true" >
        </activity>

        <!-- Definizione del File Provider del Manifest che specifica l'autorizzazione
            da utilizzare nella generazione di URI di contenuto e il nome di un file XML che indica
            le directory che l'app può condividere  -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.unipd.dei.esp22.appName"
            android:exported="false"
            android:grantUriPermissions="true">

        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/paths" />
        </provider>

        <!--
 "AR Required" app, requires "Google Play Services for AR" (ARCore)
         to be installed, as the app does not include any non-AR features.
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />
    </application>

</manifest>